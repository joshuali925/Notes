## PropTypes
- force types of props
```javascript
import React, { PropTypes } from 'react';
Component.propTypes = {
  // format is propName: PropTypes.dataType.isRequired
  age: PropTypes.number.isRequired
};
```
